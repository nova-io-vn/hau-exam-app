export function formatAcademicName(user: { fullName?: string | null; lecturerCode?: string | null; academicRank?: string | null; academicDegree?: string | null }) { const name=(user.fullName||user.lecturerCode||'Giảng viên').trim(); return [user.academicRank&&user.academicRank!=='NONE'?user.academicRank:'',user.academicDegree&&user.academicDegree!=='NONE'?user.academicDegree:'',name].filter(Boolean).join(' '); }
export function initialsFromName(user: { fullName?: string | null; lecturerCode?: string | null }) { return (user.fullName||user.lecturerCode||'GV').trim().split(/\s+/).slice(-2).map(part=>part[0]).join('').toUpperCase(); }
